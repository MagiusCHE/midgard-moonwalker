{
  "name": "moon-walker",
  "description": "Node server used to run TS script in junction with POLServer",
  "version": "0.0.1",
  "license": "MIT",
  "main": "src/app.ts",
  "nodemonConfig": {
    "env": {
      "NODE_ENV": "development"
    }
  },
  "scripts": {
    "dev": "nodemon src/app.ts",
    "test": "nodemon --help options",
    "build": "tsc"
  },
  "dependencies": {
    "@types/morgan": "^1.9.4",
    "nodemon": "^2.0.20",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  }
}